# Changelog

## 0.1.0
- Initial release, extracted from the TrainPace web and API codebases
- `verify-before-claiming` — fake-check taxonomy (silent-fallback probes, assertions that never matched, guards that fire too late), measurement fingerprinting, sweeps, negative controls, scoped claims, handoff ledger
- `project-memory` — the five repo memory artifacts, ledger entry format, promotion routing, agent roster by blast radius
- `source-of-truth` — generate / funnel / contract-test, and stable spec anchors cited from code
